<%= form_for(@organization_file) do |f| %>
  <% if @organization_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@organization_file.errors.count, "error") %> prohibited this organization_file from being saved:</h2>

      <ul>
      <% @organization_file.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
